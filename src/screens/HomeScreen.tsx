import { MapView, MyLocationBtn, SearchBar } from "../components"

export const HomeScreen = () => {
    return(
        <>
            <MapView />
            <MyLocationBtn />
            <SearchBar />
        </>
    )
}